using UnityEngine;
using System.Collections;

public class LevelCounter : MonoBehaviour {
	//public variables used to keep track of the current level and round
	public int round = 1;
	public int level = 1;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
